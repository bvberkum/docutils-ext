language: python
python:
  - "2.6"
#  - "2.7"
# command to install dependencies
branches:
  only:
  - test
  - '/^[0-9]*\.[0-9]*\.[0-9]*/'

env:
- ENV=
install:
#  - "pip install ."
#  - "pip install -r requirements.txt"
# command to run tests
#script: nosetests
