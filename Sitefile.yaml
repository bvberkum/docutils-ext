# Generic options
sitefile: 0.0.3
port: 3504
routes:
    _du: du:**/*.rst
params:
    rst2html: {}

# XXX old Du Sitefile diddles

options:
    store: 
        - 'sitedir:build/site/'
        - 'sitefile:'
        - 'sitestore:build/site.sqlite3'
    extractors:
    transforms:

Issues:
ReadMe:

# vim:ft=yaml:
