"""
- Transforms are standard Du, but can have a settings_spec like 
  components. Thus they should include SettingsSpec in their base i guess,
  but that behaviour would not be supported with std Du.

  The same settings_spec use is applied by the extractors.
"""
