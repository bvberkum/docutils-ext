"""
- These transforms are like standard Du transforms, only these can have a 
  settings_spec like Du components. 
  Thus they should include SettingsSpec in their base i guess,
  but that would not be supported with std Du.

  The same settings_spec use is applied by the extractors
  (dotmpe.du.ext.extractor).
"""
